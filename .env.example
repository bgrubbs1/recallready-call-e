# CALL-E credentials stay server-side. Never put them in browser code or commits.
CALL_E_API_KEY=

# Live outbound calls are fail-closed unless this is exactly true.
LIVE_CALLS_ENABLED=false

# Optional. Defaults to 8788.
PORT=8788
